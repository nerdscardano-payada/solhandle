export default function GrowthMeter({ label, current, baseline, target, percentage }) {
  return <div className="rounded-2xl border border-white/10 bg-slate-950/70 p-5"><div className="flex justify-between gap-3 text-sm"><strong className="text-white">{label}</strong><span className="text-cyan-200">{percentage}%</span></div><div role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={100} aria-valuenow={percentage} className="mt-4 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-emerald-300 via-cyan-300 to-violet-400" style={{ width: `${percentage}%` }}/></div><p className="mt-3 text-xs text-slate-400">{Math.max(0, current - baseline).toLocaleString()} nieuw sinds start · doel +{target.toLocaleString()}</p></div>;
}