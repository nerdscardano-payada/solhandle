import { ExternalLink } from "lucide-react";

export default function AdminClaimRequestCard({ request, busy, onMint, onReject }) {
  return <article className="rounded-xl border border-white/10 bg-slate-900/60 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="text-lg font-semibold text-white">@{request.handle}</h3><span className="rounded-full border border-amber-300/30 bg-amber-300/10 px-2 py-0.5 text-xs uppercase text-amber-200">{request.status}</span></div><p className="mt-1 text-sm text-slate-300">{request.organization} · {request.contact_name}</p><a href={`mailto:${request.contact_email}`} className="text-sm text-cyan-300">{request.contact_email}</a></div><a href={request.proof_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-sm text-violet-300"><ExternalLink className="h-4 w-4"/>Review proof</a></div><p className="mt-4 text-sm text-slate-400">{request.statement}</p><div className="mt-4 rounded-lg bg-black/20 p-3"><span className="block text-xs text-slate-500">Recipient wallet</span><code className="break-all text-xs text-slate-200">{request.recipient_wallet}</code></div>{request.status === "pending" && <div className="mt-4 flex gap-3"><button onClick={() => onMint(request)} disabled={busy} className="rounded-lg bg-emerald-300 px-4 py-2 text-sm font-semibold text-slate-950 disabled:opacity-50">{busy ? "Processing…" : "Approve & mint"}</button><button onClick={() => onReject(request)} disabled={busy} className="rounded-lg border border-rose-300/30 px-4 py-2 text-sm text-rose-200 disabled:opacity-50">Reject</button></div>}{request.mint_signature && <a href={`https://explorer.solana.com/tx/${request.mint_signature}?cluster=devnet`} target="_blank" rel="noreferrer" className="mt-4 inline-flex items-center gap-1 text-sm text-cyan-300"><ExternalLink className="h-4 w-4"/>Mint transaction</a>}</article>;
}