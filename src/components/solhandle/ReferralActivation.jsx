import { useEffect, useState } from "react";
import { Link } from "react-router-dom";

export default function ReferralActivation({ handles, onActivate, loading }) {
  const [handle, setHandle] = useState("");
  useEffect(() => { if (!handle && handles[0]?.handle) setHandle(String(handles[0].handle).replace(/^@/, "")); }, [handles, handle]);
  if (!handles.length) return <div className="card-glow mt-8"><p className="text-sm text-cyan-300">Become an Ambassador</p><h2 className="mt-2 text-2xl font-semibold text-white">Mint a SolHandle first.</h2><p className="mt-2 text-sm text-slate-400">Your connected wallet does not own a confirmed SolHandle yet. Mint one, then return with the same wallet to generate your referral link.</p><Link to="/" className="mt-5 inline-block rounded-lg bg-cyan-300 px-5 py-3 font-semibold text-slate-950">Search and mint a handle</Link></div>;
  return <div className="card-glow mt-8"><p className="text-sm text-cyan-300">Become an Ambassador</p><h2 className="mt-2 text-2xl font-semibold text-white">Choose your referral handle.</h2><p className="mt-2 text-sm text-slate-400">Choose one handle for your public referral link. No primary handle is required; ownership is verified automatically.</p><label className="mt-5 block text-xs text-slate-400">Referral handle<select value={handle} onChange={(e) => setHandle(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-slate-950 px-4 py-3 text-white sm:w-auto">{handles.map((item) => <option key={item.handle} value={String(item.handle).replace(/^@/, "")}>{item.displayHandle || `@${String(item.handle).replace(/^@/, "")}`}</option>)}</select></label><button disabled={loading || !handle} onClick={() => onActivate(handle)} className="mt-5 rounded-lg bg-cyan-300 px-5 py-3 font-semibold text-slate-950 disabled:opacity-50">{loading ? "Verifying ownership…" : "Create my referral link"}</button></div>;
}