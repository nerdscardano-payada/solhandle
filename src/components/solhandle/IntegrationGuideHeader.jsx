import { Link } from "react-router-dom";
import { ArrowLeft, ShieldCheck } from "lucide-react";

export default function IntegrationGuideHeader({ integration, type }) {
  return <section><Link to="/integrations" className="inline-flex items-center gap-2 text-sm text-slate-400 hover:text-white"><ArrowLeft className="h-4 w-4" /> Integration Center</Link><div className="mt-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-sm font-medium tracking-wider text-cyan-300">{type.title.toUpperCase()} · MAINNET GUIDE</p><h1 className="mt-2 text-4xl font-semibold md:text-5xl">{integration.name} × SolHandle</h1><p className="mt-4 max-w-2xl text-lg text-slate-300">Add {integration.capabilities.join(", ")} using the same non-custodial resolution protocol.</p></div><div className="flex items-center gap-2 rounded-full border border-amber-300/20 bg-amber-300/5 px-4 py-2 text-xs text-amber-200"><ShieldCheck className="h-4 w-4" /> Third-party product not verified</div></div><div className="mt-7 rounded-xl border border-violet-300/20 bg-violet-300/5 p-4 text-sm leading-relaxed text-violet-100"><strong>No partnership implied.</strong> This guide describes a recommended Mainnet integration for {integration.name}. Verification can only be awarded after an implementation passes the live test suite.</div></section>;
}