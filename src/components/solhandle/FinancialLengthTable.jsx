const sol = (n) => `${((n || 0) / 1e9).toFixed(3)} SOL`;
export default function FinancialLengthTable({ rows }) { return <section className="card-glow mt-6"><h2 className="font-semibold text-white">Mints by length</h2><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-xs uppercase text-slate-500"><tr><th className="py-2">Type</th><th>Mints</th><th>Base revenue</th></tr></thead><tbody>{rows.map((row)=><tr key={row.label} className="border-t border-white/5"><td className="py-3">{row.label}</td><td>{row.mints}</td><td>{sol(row.revenueLamports)}</td></tr>)}</tbody></table></div></section>; }