const tiers = [1, 2, 3, 4];

export default function AdminEarnNetworkSettings({ settings, onChange }) {
  const set = (key, value) => onChange({ ...settings, [key]: value });
  return <section className="mt-6 rounded-xl border border-violet-300/20 bg-violet-300/5 p-5"><p className="text-xs font-semibold uppercase tracking-wider text-violet-300">Earn Network launch control</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs text-slate-400">Mode<select value={settings.earn_network_mode || "PRELAUNCH"} onChange={(e) => set("earn_network_mode", e.target.value)} className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2 text-white"><option value="PRELAUNCH">Pre-launch</option><option value="LIVE">Live</option><option value="PAUSED">Paused</option></select></label><label className="text-xs text-slate-400">$HANDLE token mint<input value={settings.token_mint_address || ""} onChange={(e) => set("token_mint_address", e.target.value)} placeholder="Required before Live" className="mt-1 w-full rounded-lg border border-white/10 bg-slate-950 px-3 py-2 text-white"/></label></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{tiers.map((tier) => <div key={tier} className="rounded-lg border border-white/10 p-3"><p className="text-xs text-slate-500">Tier {tier}</p><input type="number" min="0" value={settings[`token_tier_${tier}_minimum`] ?? [25000,100000,250000,1000000][tier - 1]} onChange={(e) => set(`token_tier_${tier}_minimum`, Number(e.target.value))} className="mt-2 w-full bg-transparent text-sm text-white outline-none"/><input type="number" min="0" max="100" value={settings[`token_tier_${tier}_percentage`] ?? tier * 10 + 10} onChange={(e) => set(`token_tier_${tier}_percentage`, Number(e.target.value))} className="mt-2 w-full border-t border-white/10 bg-transparent pt-2 text-sm text-cyan-200 outline-none"/></div>)}</div><p className="mt-3 text-xs text-slate-500">First value: required $HANDLE balance. Second value: mint commission %. Live mode is rejected until a valid token mint is saved.</p></section>;
}