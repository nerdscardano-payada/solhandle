import { ExternalLink } from "lucide-react";

const sol = (lamports) => `${((lamports || 0) / 1_000_000_000).toFixed(3)} SOL`;

export default function AdminMintTable({ mints }) {
  return <section className="card-glow mt-8 overflow-hidden p-0"><div className="flex items-center justify-between border-b border-white/10 px-5 py-4"><div><h2 className="font-semibold text-white">Recent claims</h2><p className="mt-1 text-sm text-slate-500">Latest indexed on-chain mint activity</p></div></div>{!mints.length ? <p className="px-5 py-8 text-sm text-slate-400">No claims have been indexed yet.</p> : <div className="overflow-x-auto"><table className="w-full min-w-[580px] text-left text-sm"><thead className="bg-white/[0.03] text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3">Handle</th><th className="px-5 py-3">Revenue</th><th className="px-5 py-3">Minted</th><th className="px-5 py-3">Transaction</th></tr></thead><tbody>{mints.map((mint) => <tr key={mint.signature} className="border-t border-white/5 text-slate-300"><td className="px-5 py-4 font-medium text-white">{mint.handle}</td><td className="px-5 py-4">{sol(mint.priceLamports)}</td><td className="px-5 py-4">{mint.mintedAt ? new Date(mint.mintedAt).toLocaleString() : "—"}</td><td className="px-5 py-4"><a className="inline-flex items-center gap-1 text-cyan-300" href={`https://explorer.solana.com/tx/${mint.signature}?cluster=mainnet-beta`} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4" />Explorer</a></td></tr>)}</tbody></table></div>}</section>;
}