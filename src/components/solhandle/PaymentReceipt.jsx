import { CheckCircle2, ExternalLink } from 'lucide-react';
import { Link } from 'react-router-dom';
const short = value => value ? `${value.slice(0, 6)}…${value.slice(-6)}` : '—';
export default function PaymentReceipt({ payment }) {
  return <article className="card-glow border-emerald-300/30"><div className="flex items-center gap-2 text-emerald-300"><CheckCircle2 className="h-6 w-6"/><span className="text-sm font-semibold">Confirmed on Solana</span></div><h2 className="mt-3 text-3xl font-semibold text-white">{(payment.amount_lamports / 1e9).toLocaleString('en-US', { maximumFractionDigits: 9 })} SOL</h2><p className="mt-3 break-all text-sm text-slate-300">From <strong className="text-white">{payment.sender_primary_handle || short(payment.sender_wallet)}</strong> → <strong className="text-cyan-200">{payment.handle}</strong></p><div className="mt-4 space-y-2 text-xs text-slate-400"><p className="break-all">Sender wallet: {payment.sender_wallet}</p><p className="break-all">Recipient wallet: {payment.receiver_wallet}</p><p className="break-all">Signature: {payment.tx_signature}</p><p>Status: {payment.status} · {new Date(payment.confirmed_at || payment.created_at).toLocaleString()}</p></div><div className="mt-5 flex flex-wrap gap-3 text-sm"><a href={`https://explorer.solana.com/tx/${payment.tx_signature}`} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 font-semibold text-cyan-300">View on Explorer <ExternalLink className="h-4 w-4"/></a><Link to="/pay" className="text-slate-300 hover:text-white">Send another payment</Link></div></article>;
}