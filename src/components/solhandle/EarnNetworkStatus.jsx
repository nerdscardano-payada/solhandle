export default function EarnNetworkStatus({ settings, profile }) {
  const prelaunch = settings?.mode !== "LIVE";
  return <div className={`mt-6 rounded-xl border p-4 ${prelaunch ? "border-violet-300/30 bg-violet-300/10" : "border-emerald-300/30 bg-emerald-300/10"}`}><div className="flex flex-wrap items-center justify-between gap-3"><div><p className={`text-xs font-semibold uppercase tracking-wider ${prelaunch ? "text-violet-200" : "text-emerald-200"}`}>{prelaunch ? "Pre-launch waiting mode" : "Earn Network live"}</p><h3 className="mt-1 font-semibold text-white">{prelaunch ? "$HANDLE token ID pending" : `${Number(profile?.earning_tier_percentage || 0)}% mint tier active`}</h3></div><span className="rounded-full border border-white/10 bg-slate-950/60 px-3 py-1 text-xs text-slate-300">{prelaunch ? "Claims locked" : `${Number(profile?.token_balance_cached || 0).toLocaleString()} $HANDLE`}</span></div><p className="mt-2 text-sm text-slate-400">{prelaunch ? "Origin referrals and network activity are recorded now. No earnings accrue and no claims open until the token mint is configured and the network is activated." : `Tier upgrades qualify after ${settings?.qualificationHours || 24} hours; downgrades apply immediately.`}</p></div>;
}