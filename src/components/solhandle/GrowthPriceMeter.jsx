const compactUsd = value => Number.isFinite(value) && value > 0 ? `$${new Intl.NumberFormat('en-US', { notation: 'compact', maximumFractionDigits: 1 }).format(value)}` : '—';
export default function GrowthPriceMeter({ current, target, percentage, source = 'snapshot' }) {
  return <div className="rounded-2xl border border-white/10 bg-slate-950/70 p-5"><div className="flex justify-between gap-3 text-sm"><strong className="text-white">$HANDLE market cap <span className="text-xs font-normal text-slate-400">{source === 'live' ? 'Live · ±30 sec' : source === 'stale' ? 'Laatst opgehaald' : 'Uurlijkse meting'}</span></strong><span className="text-cyan-200">{percentage.toFixed(1)}% van doel</span></div><div role="progressbar" aria-label="$HANDLE market-capdoel" aria-valuemin={0} aria-valuemax={100} aria-valuenow={percentage} className="mt-4 h-2 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-r from-emerald-300 via-cyan-300 to-violet-400" style={{ width: `${percentage}%` }}/></div><p className="mt-3 text-xs text-slate-400">MC nu {compactUsd(current)} · doel {compactUsd(target)}</p><p className="mt-1 text-xs text-slate-500">{source === 'live' ? 'Indicatief: officiële mijlpalen gebruiken de uurlijkse meting.' : 'Live market cap tijdelijk niet beschikbaar; officiële mijlpalen gebruiken de uurlijkse meting.'}</p></div>;
}