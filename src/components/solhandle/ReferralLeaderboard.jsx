import { useEffect, useState } from "react";
import { base44 } from "@/api/base44Client";
import { sol } from "@/lib/referralLevels";

export default function ReferralLeaderboard() {
  const [leaders, setLeaders] = useState([]); const [period, setPeriod] = useState("7d");
  useEffect(() => { base44.functions.invoke("referralPortal", { action: "leaderboard", period }).then((res) => setLeaders(res.data.leaders || [])).catch(() => setLeaders([])); }, [period]);
  return <section className="card-glow mt-8"><p className="text-sm text-violet-300">Confirmed mints only</p><div className="mt-2 flex flex-wrap items-center justify-between gap-3"><h2 className="text-2xl font-semibold text-white">Top Ambassadors</h2><div className="flex rounded-lg border border-white/10 p-1">{[["24h", "24H"], ["7d", "7D"], ["all", "All Time"]].map(([value, label]) => <button key={value} onClick={() => setPeriod(value)} className={`rounded-md px-3 py-1.5 text-xs ${period === value ? "bg-violet-400/20 text-violet-200" : "text-slate-500"}`}>{label}</button>)}</div></div>{leaders.length ? <div className="mt-5 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="text-slate-500"><tr><th className="pb-2">Rank</th><th>Ambassador</th><th>Mints</th><th>Earned</th></tr></thead><tbody>{leaders.map((item, index) => <tr key={`${item.handle}-${index}`} className="border-t border-white/10"><td className="py-3">{index + 1}</td><td className="font-semibold text-white">{item.handle}</td><td>{item.referrals}</td><td className="text-emerald-300">{sol(item.earnedLamports)}</td></tr>)}</tbody></table></div> : <p className="mt-5 text-sm text-slate-500">The leaderboard starts with the first confirmed referrals in this period.</p>}</section>;
}