const rewards = ["Community milestone", "Visibility campaign", "$HANDLE Buyback & Burn", "Share & Earn bonus event", "Visibility campaign", "$HANDLE Buyback & Burn", "Community reward", "Visibility campaign", "$HANDLE Buyback & Burn", "Cycle complete"];
export default function GrowthMilestones({ progress, cycle }) {
  return <section className="mt-10"><h2 className="text-2xl font-semibold text-white">Milestones</h2><p className="mt-2 text-sm text-slate-400">Reaching a milestone does not mean its reward has been funded or delivered. Rewards are announced separately when a budget and proof are available.</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{rewards.map((reward, index) => { const threshold = (index + 1) * 10; const reached = progress >= threshold; return <div key={threshold} className="flex items-center gap-4 rounded-xl border border-white/10 bg-slate-950/70 px-4 py-3"><span className={reached ? "font-mono text-emerald-300" : "font-mono text-slate-500"}>{threshold}%</span><div><p className="text-sm font-medium text-white">{reward}</p><p className="text-xs text-slate-400">{reached ? "Reached · reward pending confirmation" : `Locked · Cycle ${cycle}`}</p></div></div>; })}</div></section>;
}