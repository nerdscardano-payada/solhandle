import { useState } from "react";
import { ArrowLeft, ArrowRight, CheckCircle2 } from "lucide-react";
import { getWizardSteps } from "@/lib/integrationWizardContent";

export default function IntegrationWizard({ integration }) {
  const [current, setCurrent] = useState(0);
  const steps = getWizardSteps(integration);
  const step = steps[current];
  const isLast = current === steps.length - 1;
  return <section className="mt-12"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold tracking-wider text-cyan-300">INTERACTIVE IMPLEMENTATION WIZARD</p><h2 className="mt-2 text-2xl font-semibold">Integrate {integration.name}</h2></div><p className="text-sm text-slate-500">Step {current + 1} of {steps.length}</p></div><div className="mt-5 flex gap-2" aria-label="Wizard progress">{steps.map((item, index) => <button key={item.title} type="button" onClick={() => setCurrent(index)} aria-label={`Open step ${index + 1}: ${item.title}`} className={`h-1.5 flex-1 rounded-full ${index <= current ? "bg-cyan-300" : "bg-white/10"}`} />)}</div><div className="mt-5 min-h-[300px] rounded-2xl border border-white/10 bg-slate-950/70 p-5 md:p-7"><p className="text-xs font-semibold tracking-wider text-violet-300">{step.eyebrow}</p><div className="mt-4 flex items-start gap-4"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-cyan-300/10 text-sm text-cyan-200">{current + 1}</span><div><h3 className="text-xl font-semibold text-white">{step.title}</h3><p className="mt-3 max-w-3xl leading-relaxed text-slate-300">{step.body}</p>{step.where && <div className="mt-5 grid gap-3 md:grid-cols-2"><div className="rounded-xl border border-cyan-300/15 bg-cyan-300/5 p-4"><p className="text-xs font-semibold tracking-wider text-cyan-300">WHERE?</p><p className="mt-2 text-sm leading-relaxed text-slate-300">{step.where}</p></div><div className="rounded-xl border border-violet-300/15 bg-violet-300/5 p-4"><p className="text-xs font-semibold tracking-wider text-violet-300">WHY?</p><p className="mt-2 text-sm leading-relaxed text-slate-300">{step.why}</p></div></div>}{step.detail && <p className="mt-4 text-sm text-slate-500">{step.detail}</p>}</div></div>{step.code && <div className="mt-6 rounded-xl border border-white/10 bg-black/40 p-4"><div className="mb-3 text-xs font-semibold tracking-wider text-slate-500">CODE TO ADD</div><pre className="overflow-x-auto text-sm leading-relaxed text-cyan-100"><code>{step.code}</code></pre></div>}{isLast && <div className="mt-6 flex items-center gap-2 rounded-xl border border-amber-300/20 bg-amber-300/5 p-4 text-sm text-amber-100"><CheckCircle2 className="h-4 w-4" /> Ready to submit after the Mainnet verification service opens.</div>}</div><div className="mt-4 flex items-center justify-between"><button type="button" onClick={() => setCurrent((value) => value - 1)} disabled={current === 0} className="inline-flex items-center gap-2 rounded-full border border-white/10 px-4 py-2 text-sm text-slate-300 disabled:cursor-not-allowed disabled:opacity-30"><ArrowLeft className="h-4 w-4" /> Previous</button><button type="button" onClick={() => setCurrent((value) => Math.min(value + 1, steps.length - 1))} disabled={isLast} className="inline-flex items-center gap-2 rounded-full bg-cyan-300 px-4 py-2 text-sm font-medium text-slate-950 disabled:cursor-not-allowed disabled:opacity-30">Next step <ArrowRight className="h-4 w-4" /></button></div></section>;
}