import Header from "@/components/solhandle/Header";
const topics=["What is SolHandle?","How ownership works","How minting works","Transfers & marketplaces","Protected handles","No renewals","Security","Developer API"];
export default function Docs() { return <main className="min-h-screen bg-[#050811] text-white"><div className="mx-auto min-h-screen max-w-7xl border-x border-white/10"><Header/><section className="grid gap-10 px-5 py-12 md:grid-cols-[220px_1fr] md:px-9"><aside className="space-y-3">{topics.map((topic,i)=><a href={`#${i}`} key={topic} className="block text-sm text-slate-400 hover:text-cyan-200">{topic}</a>)}</aside><article className="max-w-3xl"><p className="text-sm text-cyan-300">Documentation</p><h1 className="mt-2 text-4xl font-semibold">Your @ on Solana.</h1><p className="mt-5 text-lg leading-relaxed text-slate-300">A SolHandle is a human-readable identity represented by a Solana Core Asset. The person who owns the NFT owns the handle.</p>{topics.map((topic,i)=><section id={`${i}`} key={topic} className="mt-10 border-t border-white/10 pt-7"><h2 className="text-xl font-semibold">{topic}</h2><p className="mt-3 leading-relaxed text-slate-400">SolHandle resolves <strong className="text-white">@ansem</strong> by deriving its deterministic registry record and reading the current owner of the official collection asset. The blockchain is the authority; the website is an index built for speed.</p></section>)}</article></section></div></main>;
}