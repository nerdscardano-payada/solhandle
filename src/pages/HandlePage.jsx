import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import Header from "@/components/solhandle/Header";
import { base44 } from "@/api/base44Client";
import { normalizeHandle, shortenAddress } from "@/lib/solhandle";
export default function HandlePage() { const handle=normalizeHandle(window.location.pathname); const [data,setData]=useState(null); useEffect(()=>{base44.functions.invoke("getHandleAvailability",{handle}).then(res=>setData(res.data));},[handle]); return <main className="min-h-screen bg-[#050811] text-white"><div className="mx-auto min-h-screen max-w-7xl border-x border-white/10"><Header/><section className="mx-auto max-w-3xl px-5 py-16 text-center"><div className="mx-auto grid h-24 w-24 place-items-center rounded-3xl bg-gradient-to-br from-emerald-300 via-cyan-400 to-violet-500 text-5xl font-bold text-slate-950">@</div><h1 className="mt-6 text-5xl font-semibold">@{handle}</h1><span className="mt-3 inline-block rounded-full bg-emerald-400/10 px-3 py-1 text-sm text-emerald-300">Official SolHandle ✓</span><div className="card-glow mt-10 text-left"><Detail label="Status" value={data?.available ? "Available" : data?.status || "Indexed on-chain"}/><Detail label="Current owner" value={shortenAddress(data?.currentOwner)}/><Detail label="Collection" value="SOLHANDLE Core Collection"/><Detail label="Handle length" value={`${handle.length} characters`}/></div><Link to="/" className="mt-8 inline-block text-cyan-200">Search another handle</Link></section></div></main> }
function Detail({label,value}) { return <div className="flex justify-between border-b border-white/10 py-4 last:border-0"><span className="text-slate-400">{label}</span><span className="text-white">{value}</span></div> }